services:
  - type: web
    name: my-python-app
    env: python
    # Change this line to a stable version
    pythonVersion: "3.11" # Or "3.12"
    buildCommand: "pip install -r requirements.txt"
    startCommand: "gunicorn myapp.wsgi"
